# Game Dev Agent Studio — 工业级依赖清单
# 核心架构采用零强依赖设计，核心工具可脱离第三方包极速冷启动。
# 涉及特定子系统（DSL/行为树/图像审计/LLM云端网关）需安装对应依赖：

# ─── 核心引擎与 DSL 必需依赖 ───────────────────────────────────────────────
PyYAML>=6.0.0                 # DSL 语法解析、行为树、帧数据表、叙事事件图必需

# ─── LLM Provider SDK（可选，未配置自动降级到本地模板模式） ─────────────────
google-genai>=1.0.0           # Google Gemini 2.0/2.5
openai>=1.0.0                 # OpenAI GPT-4o / DeepSeek API
anthropic>=0.20.0             # Claude 3.5 Sonnet/Opus
httpx>=0.24.0                 # Ollama 本地模型异步 HTTP 通信

# ─── 图像与多模态审计（可选增强） ─────────────────────────────────────────────
pillow>=10.0.0                # 图像处理与 VLM 视觉审美截图分析

# ─── 配置管理 ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # .env 环境变量自动加载
